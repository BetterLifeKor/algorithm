// Date   : 2017.9.20.
// Title  : 
// Editor by BetterLife

// _MBCS;%(PreprocessorDefinitions);_CRT_SECURE_NO_WARNINGS

#include <iostream>
#include <string>
#include <vector>
#include <queue>
#include <map>
#include <algorithm>	// sort
#include <utility>		// pair
#include <cmath>		// pow(), sqrt()

#define EPSILON 1e-9
#define INF 1e10
#define MOD 1000000007
#define FOR(i,n) for(int i =0; i<n; i++)
#define FOR2(i,n) for(int i = 1; i<=n; i++)

using namespace std;

// ************** typedef(start) ************** //
typedef long long lint;

typedef vector<int> vi;
typedef vector<vector<int>> vvi;

typedef pair<int, int> pii;
typedef vector<pair<int, int>> vpii;

// ************** typedef(end) ************** //

// ************** struct(start) ************** //
typedef struct point {
	//char name[20]; // Point p; strcpy(p.name, "BetterLife");
	int x;
	int y;
	int index = 0;
}Point;


// ************** struct(end) ************** //

// ************** Global Variable(start) ************** //
int n;
int m;
vi myVector1;
vvi myVector2;
// ************** Global Variable(end) ************** //

int main() {
	//ios_base::sync_with_stdio(false);
	int T; scanf("%d", &T); while (T--) {
		scanf("%d", &n);
		scanf("%d %d", &n, &m);

		myVector1.clear();
		myVector1 = vi(n);
		FOR(i, n) scanf("%d", &myVector1[i]);

		myVector2.clear();
		myVector2 = vvi(n, vi(m));
		FOR(i, n) FOR(j, m)	scanf("%d", &myVector2[i][j]);

	}
	return 0;
}